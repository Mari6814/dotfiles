export BASH_PROFILE="$HOME/.bash_profile"
case $- in *i*) echo "bash_profile: $BASH_PROFILE";; esac

if [ -f ~/.local/bash_profile ]; then
    . ~/.local/bash_profile
fi

if [ -f ~/.profile ]; then
    . ~/.profile
fi

if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi
